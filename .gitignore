# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env / secrets
.env
.env.local
.env.*.local
*.key
*.pem
service-account*.json

# Per-deployment config (deploy.example.json은 커밋, deploy.json은 사용자 환경)
deploy.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Build / cache
build/
dist/
coverage/
.cache/
*.log
*.tsbuildinfo

# FSD 런타임 산출물 (재실행 시 덮어써짐 — 영속 회귀 자산은 tests/*.test.mjs)
tests/test-results.json
.fsd/

# Project-local (프로젝트별 현장문서 RAG 캐시)
data/cache/
data/embeddings-cache/

# 내부 문서 (외부 비공개 — 외부용은 AGENTS.md/ROADMAP.md/CONTRIBUTING.md 로 대체)
CLAUDE.md
dev.md
plan.md
