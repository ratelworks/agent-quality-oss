# Security Policy

## 보안 정책

**agent-quality-oss** 는 한국 건설 품질관리 도메인 지식(공종·자재·시험·기준·부적합·법령·양식의 관계망)을 LLM 에 공급하는 MCP 서버입니다. 본 OSS 는 도메인 지식 *공급* 레이어이므로, 사용자의 현장 데이터를 자동으로 외부로 보내지 않는 것이 본질적 의무입니다.

---

## 1. 지원 버전

| 버전 | 보안 패치 지원 | 비고 |
|---|:--:|---|
| 0.3.x | ✅ | 현재 공개 준비판 (v0.3.0) |

> 0.x 개발 단계에서는 최신 minor 만 보안 패치를 지원합니다. 1.0 이후 LTS 정책을 본 표에 명시합니다.

---

## 2. 취약점 신고

보안 취약점을 발견하시면 **공개 issue 가 아닌** 다음 경로로 신고해 주세요:

- 이메일: `alphamale@ratelworks.co.kr` (제목: `[SECURITY] agent-quality-oss`)
- 연락 정보: ㈜라텔웍스 (Ratelworks Inc.)

48 시간 내 1차 회신, 7 일 내 분석 및 수정 일정 공유합니다.

---

## 3. 데이터 처리

### 3.1 무엇을 다루나

- **번들 온톨로지** (`src/ontology/data/*.json`): 공종·자재·시험·기준·부적합·법령·양식의 **식별자와 구조 메타데이터**. KCS/KDS/KS 등 외부 기준의 **원문 텍스트는 포함하지 않습니다** ([`NOTICE.md`](./NOTICE.md) 참조).
- **사용자 입력**: 도구 호출 시 전달되는 공종·관측값·프로젝트 컨텍스트 등. 본 서버는 이를 메모리 내 추론에만 사용하며 영구 저장하지 않습니다.

### 3.2 처리 원칙

- ✅ 기본 동작: 모든 추론은 로컬(사용자 머신 또는 사용자가 운영하는 서버)에서 수행됩니다.
- ✅ 자동 외부 전송 없음: 본 OSS 는 사용자 데이터를 외부 서버로 자동 전송하지 않습니다. 서드파티 분석·추적 코드를 포함하지 않습니다.
- ✅ 현장문서 RAG (Phase 2+, 로드맵): 사용자가 **로컬에 업로드한 원문**에 대해서만 동작하도록 설계됩니다. 업로드 문서의 보존·삭제·마스킹은 사용자 책임입니다.

### 3.3 사용자 책임 영역

| 항목 | 사용자 책임 |
|---|---|
| 현장 식별 정보 (현장명·담당자·시험성적서 등) | 도구 호출 payload·로그에 포함 시 사용자가 마스킹·관리 |
| Phase 2+ 업로드 원문의 보존·삭제 | 법정 보존 기간 준수 및 만료 후 삭제는 사용자 책임 |
| 디스크 암호화 | OS 수준 (macOS FileVault / Windows BitLocker / Linux LUKS) 권장 |

---

## 4. 외부 통신

- **런타임**: 현재 버전(v0.3.0)은 도구 응답 생성을 위해 **외부 서버와 통신하지 않습니다.** 모든 데이터는 번들 온톨로지에서 조회합니다.
- **HTTP JSON 모드**: 사용자가 직접 `npm start` 로 띄우는 서버(`/mcp/tools`)는 사용자 인프라에서 동작하며, 본 OSS 가 별도 외부로 데이터를 송신하지 않습니다.
- **빌드/동기화 스크립트**(`scripts/`): 법제처 등 공개 출처에서 법령 메타를 받아오는 동기화 도구는 **메인테이너의 빌드타임 전용**이며, 런타임이나 npm 패키지 동작에 포함되지 않습니다.
- **Phase 3+ (로드맵)**: LLM·RAG 백엔드 연동 시, 외부 전송 항목과 opt-in 절차를 본 문서에 명시한 뒤 도입합니다.

---

## 5. 의존성 보안

```bash
npm audit
```

`npm audit --audit-level=high` 단계가 CI workflow(`.github/workflows/ci.yml`)에서 PR / push 시 자동 차단합니다. 또한 Dependabot(`.github/dependabot.yml`)이 주간 보안 패치 PR 을, CodeQL(`.github/workflows/codeql.yml`)이 정적 분석 회귀를 차단합니다.

---

## 6. 시크릿 관리

- `.env` 파일을 저장소에 커밋하지 않습니다(OSS). 환경변수는 런타임에 주입합니다.
- 클라우드 배포 시 Firestore/Vertex AI 등의 자격증명은 서비스 계정 ADC 로 해결하며, npm 패키지에 포함되지 않습니다.

---

## 7. 공개 위생 게이트

`prepublishOnly` 와 CI 에서 자동 실행 (`package.json` 정의):

```bash
npm run check:oss-hygiene   # 내부 용어 / 내부 경로 / 개인정보 노출 차단
npm run release:notes       # Release 노트 SSoT(CHANGELOG) 게이트
```

외부 공개물(README/CHANGELOG/docs)에 내부 운영 용어·내부 경로·개인정보가 새지 않도록 publish·merge 자체를 차단합니다.

---

## 8. 책임 한계

본 OSS 는 **법적 강제 효력이 없는 검색·인용·작성 보조 도구**입니다. 다음은 사용자 책임입니다:

- 작성된 품질문서의 검토·승인
- 정부·발주처 제출 책임
- 현장 품질 의사결정 및 부적합 판정
- 사고·하자 발생 시 책임

LLM 의 자동 작성 결과는 항상 **담당 품질관리자(QC)·감리원이 검토 후 승인**해야 합니다. 정량 판정은 프로젝트 배합설계서·시방서가 번들 공개기준에 우선합니다.

---

문의: `alphamale@ratelworks.co.kr` · ㈜라텔웍스
